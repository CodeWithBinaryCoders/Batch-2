#include <stdio.h>
#include <limits.h>

int main()
{
    int max = INT_MAX;
    int min = INT_MIN;

    printf("max int value = %d\n", max);
    printf("min int value = %d\n", min);

    return 0;
}

