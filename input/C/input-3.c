#include <stdio.h>

int main()
{
    int x;
    printf("Enter value for x :\n");
    scanf("%d", &x); // user inputs a value (let's say 10)

    // now value of x is 10

    printf("Value of x is %d\n", x);

    return 0;
}