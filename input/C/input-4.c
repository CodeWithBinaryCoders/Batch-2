#include <stdio.h>

int main()
{
    int x, y, z;

    // scanf("%d", &x);
    // scanf("%d", &y);
    // scanf("%d", &z);
    scanf("%d%d%d", &x, &y, &z);

    printf("Values of x, y, z are %d, %d, %d respectively.\n", x, y, z);

    return 0;
}