#include <stdio.h>

int main()
{
    int i = 10;

    while (i) // while (i != 0)
    // true for every non-zero value
    // {
        // printf("%d ", i);
        // i--;
        printf("%d ", i--); 
    // }

    printf("\n");

    return 0;
}

